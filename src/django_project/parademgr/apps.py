from django.apps import AppConfig


class App0Config(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "parademgr"

    # def ready(self):
    #     import parademgr.signals
